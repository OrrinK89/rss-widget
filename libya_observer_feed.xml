<!DOCTYPE html>
<html>
<head>
    <title>Libya Observer RSS Feed</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
        }
        .rss-feed {
            margin: 20px;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            background-color: #f9f9f9;
        }
        .rss-item {
            margin-bottom: 15px;
            padding: 10px;
            border-bottom: 1px solid #ccc;
        }
        .rss-item a {
            font-weight: bold;
            color: #007BFF;
            text-decoration: none;
        }
        .rss-item a:hover {
            text-decoration: underline;
        }
        .rss-item p {
            margin: 5px 0;
            color: #555;
        }
    </style>
</head>
<body>
    <h1>Libya Observer News Feed</h1>
    <div id="rss-feed" class="rss-feed">Loading...</div>

    <script>
        async function fetchRSSFeed() {
            try {
                const response = await fetch("https://<yourusername>.github.io/<repositoryname>/libya_observer_feed.xml");
                const text = await response.text();
                const parser = new DOMParser();
                const xml = parser.parseFromString(text, "text/xml");

                const items = xml.querySelectorAll("item");
                const feedContainer = document.getElementById("rss-feed");

                feedContainer.innerHTML = Array.from(items)
                    .map(item => {
                        const title = item.querySelector("title").textContent;
                        const link = item.querySelector("link").textContent;
                        const description = item.querySelector("description").textContent;

                        return `
                            <div class="rss-item">
                                <a href="${link}" target="_blank">${title}</a>
                                <p>${description}</p>
                            </div>
                        `;
                    })
                    .join("");
            } catch (error) {
                document.getElementById("rss-feed").innerText = "Failed to load RSS feed.";
                console.error("Error fetching RSS feed:", error);
            }
        }

        fetchRSSFeed();
    </script>
</body>
</html>
